# define hosts, in this case probably just fs storage. since this is being deployed on a Freeside server I will assume there is an ssh config located in `~/.ssh/config` which contains the IP address of the machine as well as a username and prefered authentication method(s)

my_hosts = ["fs-storage"]
